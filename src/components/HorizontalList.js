import styled from 'styled-components';

export default styled.div`
  display: flex;
  flex: 1;
  overflow: hidden;
  overflow-x: scroll;
  padding: 4px;
  margin: -4px;
  align-items: flex-start;
`;
